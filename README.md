# basic_dev_setup

**TODO** create a proper documentation here

## Installation

here only same brain dump notes

- wsl installation (for windows systems) -> docker already included
- add GitHub PAT to clone the repo
- (perhaps) start vs code from within wsl
- (for gtest baremetal only) add ssh keys to clone the baremetal repo????